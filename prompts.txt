The aim 01: Define all TypeScript interfaces for the DummyJSON products API response
Actual prompt: I need strict TypeScript interfaces for the DummyJSON /products endpoint. The response contains: id, title, description, category, price, discountPercentage, rating, stock, tags (string[]), brand, sku, weight, dimensions (width/height/depth), warrantyInformation, shippingInformation, availabilityStatus, reviews (array of {rating, comment, date, reviewerName, reviewerEmail}), returnPolicy, minimumOrderQuantity, meta (createdAt, updatedAt, barcode, qrCode), images (string[]), thumbnail. Please give me strict interfaces with no 'any' types. Also add interfaces for the auth login response, cart item state, filter state, and a SortOption union type.

The aim 02: Build a reusable Pinia cart store with localStorage persistence
Actual prompt: Create a Pinia store in Vue 3 (Composition API style with defineStore) for a shopping cart. Requirements: 1) CartItem type has { product: Product, quantity: number }, 2) Items must persist to localStorage on every change, 3) Load from localStorage on startup, 4) Expose: items, totalItems (computed), totalPrice (computed), isEmpty (computed), addToCart(product), removeFromCart(productId), updateQuantity(productId, qty), clearCart(), isInCart(productId), getQuantity(productId). Use strict TypeScript, no any.

The aim 03: Implement authentication with DummyJSON /auth/login storing JWT in localStorage
Actual prompt: Create a Pinia auth store for a Vue 3 app. It should call POST https://dummyjson.com/auth/login with { username, password, expiresInMins: 60 }. On success, store the JWT token and full user object in localStorage. Expose: user (ref), loading, error, isLoggedIn (computed), token (computed from localStorage), displayName (computed as firstName + lastName), login(credentials) async function returning boolean, logout() function that clears localStorage. Fully typed in TypeScript.

The aim 04: Build a reactive FilterBar component with v-model support
Actual prompt: Build a Vue 3 FilterBar component using Tailwind CSS that accepts a FilterState v-model ({ search, category, sortBy, minPrice, maxPrice }) and a categories string array prop. It should have: a search input with a search icon, a category <select>, a sort <select> with options (default, price-asc, price-desc, rating-desc, name-asc), active filter pills that show what's currently filtered with an 'x' to remove each, and a 'Clear all' button that only shows when filters are active. Use the emit pattern for v-model updates. Fully typed TypeScript with no any.

The aim 05: Create the Tailwind config with custom tokens for the grocery theme
Actual prompt: Write a tailwind.config.js for a grocery store SPA. I want: 1) darkMode: 'class', 2) A custom 'fresh' color palette (greens, shades 50–900), 3) An 'earth' color palette (warm yellows), 4) Custom fonts: 'Playfair Display' as display font, 'DM Sans' as body font, 5) Custom animations: fade-up, fade-in, slide-in, bounce-sm, 6) Custom keyframes for those animations, 7) Custom box shadows: card, card-hover, modal. Also write the accompanying global CSS file with @tailwind directives, custom scrollbar styles, and reusable @layer components utility classes for .btn-primary, .btn-secondary, .btn-ghost, .badge, .card, .input.

The aim 06: Set up Vue Router with lazy loading and smooth page transitions
Actual prompt: Set up Vue Router 4 for a Vue 3 TypeScript app with these routes: '/' → HomeView (lazy), '/product/:id' → ProductView (lazy, with props: route => ({ id: Number(route.params.id) })), '/cart' → CartView (lazy), '/login' → LoginView (lazy), '/:pathMatch(.*)*' → NotFoundView. Use createWebHistory. Add a scrollBehavior that uses savedPosition if available, otherwise scrolls to top smoothly. Export as default. Fully typed with RouteRecordRaw.

The aim 07: Implement product filtering and sorting logic in HomeView
Actual prompt: In a Vue 3 Composition API component, I have an allProducts ref of type Product[]. I need a filteredProducts computed property that: 1) filters by a search string matching title, description, category, and tags, 2) filters by an exact category string match, 3) sorts by sortBy which can be 'price-asc', 'price-desc', 'rating-desc', 'name-asc', or 'default'. Then paginate the result with a PAGE_SIZE of 20. Also compute totalPages and paginatedProducts (slice of filteredProducts). Use a visiblePages computed that shows pages within ±2 of currentPage. All strictly typed, no any.

The aim 08: Style the dark mode NavBar with responsive mobile menu
Actual prompt: Build a Vue 3 NavBar component with Tailwind CSS. Requirements: sticky top-0 with backdrop blur, dark mode support using dark: classes, logo on the left, nav links in the middle (hidden on mobile), right side has: dark mode toggle button (emoji ☀️/🌙), cart icon with an animated badge showing item count, auth state (show user avatar + logout if logged in, show Sign In button if not), mobile hamburger menu that expands with a slide animation. Use Vue Router RouterLink for navigation. The cart count badge should have a bounce animation when items are added. Fully typed TypeScript.
